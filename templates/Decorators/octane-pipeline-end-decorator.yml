steps:
- ${{ if eq(variables['InjectAlmOctaneTasks'], 'true') }}:
  - task: ALMOctane.alm-octane-integration.octane-end-task.octane-end-task@1
    condition: always()
    displayName: 'ALM Octane Job End'
    inputs:
      OctaneServiceConnection: 'Octane'